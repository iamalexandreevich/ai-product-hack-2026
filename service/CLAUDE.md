# service/ — правила работы

## Границы (жёсткое правило)

**Работаем только внутри `service/`.** Папки `docs/`, `contracts/`, `adapters/`, `benchmark/` и файлы в корне репозитория (`README.md`, `.gitignore`, `CLAUDE.md`) — вне зоны изменений. Не создавать, не редактировать, не удалять в них ничего.

Следствия:
- Никогда не запускать `git add -A`, `git add .` или `git commit -a`. Только явные пути внутри `service/`.
- В корне репозитория лежит незакоммиченная работа пользователя (`README.md`, `docs/base.md`, `docs/auto-mode-industry-review-2026.md`, `docs/best-practices.md`) и служебный каталог `.superpowers/`. Всё это не наше — не трогать и не коммитить.
- Если задача требует файла вне `service/` — не делать её молча, а сообщить и остановиться.

## Что здесь строится

Ядро AgentGate: FastAPI-сервис `POST /v1/decide`, каскад «детерминированная ступень 1 → LLM-ступень 2».

- Спека: `docs/superpowers/specs/2026-09-03-agentgate-v1-design.md` (читать можно, менять нельзя)
- План: `docs/superpowers/plans/2026-09-03-agentgate-v1.md` (читать можно, менять нельзя)

## Технические правила

- Python `>=3.12`, зависимости через `uv`. Все команды — из `service/`, вида `uv run …`.
- Код и комментарии — английский. Документация и README — русский. Идентификаторы API не переводятся.
- TDD обязателен: сначала падающий тест, потом реализация. Тест, который не падал до реализации, не считается тестом.
- Fail-closed по всему сервису: любая ошибка, таймаут, невалидный ответ или невалидный запрос → `ask` с HTTP 200. `allow` по ошибке невозможен; на каждый путь отказа есть тест.
- Решение по сырой строке команды запрещено везде; только по `NormalizedAction`.
- `deny` и `ask` не кэшируются; кэшируется только `allow`.
- Хранилище — только Postgres (asyncpg). SQLite не поддерживается.
- Ретраев к LLM нет: один вызов, один таймаут.

## Коммиты

Коммит после каждой задачи, только явные пути внутри `service/`. Сообщение заканчивается строкой:

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
```
