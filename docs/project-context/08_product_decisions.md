# Product Decisions

Status: Intermediate / Work in Progress

> Дата: 2026-09-04, ветка `main`.
>
> Документ фиксирует **текущее состояние продуктовых решений команды**, а не историю обсуждений и не финальную продуктовую стратегию. Часть решений уже подтверждена работающим кодом, часть остаётся гипотезой, часть — предположением, не проверенным ничем.
>
> Источники: `01_hackathon_case.md`, `02_a_competitor_research.md`, `02_b_competitor_research.md`, `03_competitor_research_summary.md`, `04_architecture/architecture_description.md`, `05_current_state.md`, `06_benchmark_status.md`, `07_team.md`, `docs/why-agentgate.md`, кодовая база `service/`, `contracts/`, `adapters/`, `benchmark/`.
>
> **Обозначения статуса утверждений** — используются по всему документу:
> - **[Ф]** — факт: подтверждён кодом, прогоном, кейсом хакатона или внешним исследованием (с указанием источника);
> - **[Р]** — решение: команда сознательно выбрала этот вариант, выбор зафиксирован в спеке, коде или архитектуре;
> - **[Г]** — гипотеза: проверяемое утверждение, которое команда намерена проверить бенчмарком, интеграцией или демонстрацией;
> - **[П]** — предположение: принято как рабочее, способ проверки пока не определён;
> - **[?]** — не подтверждённый результат: то, что нельзя утверждать сегодня.
>
> **Оговорка, важная для чтения всего документа.** Пользовательских исследований (интервью, опросов, наблюдения за сессиями) команда **не проводила** — их нет ни в одном из источников. Все утверждения о пользователе опираются либо на формулировку кейса, либо на внешние исследования из конкурентного анализа, либо на рассуждение команды. Там, где ниже говорится о поведении пользователя, это помечено как **[П]** или **[Г]**, а не как знание.
>
> Вторая оговорка: **измерений бенчмарка не существует.** Конвейер готов и исполняется, датасет из 75 кейсов валиден, 122 собственных теста проходят — но против живого AgentGate бенчмарк не запускался ни разу (`06_benchmark_status.md`, «Current results»). Поэтому ни одной цифры ASR / Utility / FP / Friction / Latency в этом документе нет и быть не может.

---

## 1. Target user

### Primary user

**[Р]** Основной пользователь — **разработчик, который уже работает с кодинг-агентом в CLI или IDE и уже столкнулся с выбором между подтверждением каждого шага и полным отключением проверок.**

Уточнение, которое команда считает существенным для приоритизации:

**[Р]** В качестве первичного сегмента выбран **разработчик, использующий хотя бы один открытый кодинг-агент** (Kilo Code, OpenCode, Cline/Roo, OpenHands) — один или в небольшой команде.

**Контекст использования** — **[Ф]** из `01_hackathon_case.md`: агент пишет код, ставит зависимости, запускает тесты, нередко сам разворачивает приложение; человек валидирует результат «на вайбе». Продуктивность резко растёт именно тогда, когда агенту дают действовать без подтверждения каждого шага.

**Почему проблема релевантна именно этому сегменту:**

- **[Ф]** У закрытых вендоров auto mode уже есть: Claude Code (по умолчанию с 14 августа 2026), Codex Auto-review, Cursor Run Modes. В открытых харнессах сопоставимого движка нет: в Kilo Code auto mode только проектируется (issue #9138), в Kiro открыт запрос. Источник — кейс хакатона и `03_competitor_research_summary.md` §1.
- **[Ф]** Следствие: у пользователя открытого агента выбор сегодня бинарный — approval fatigue или `--dangerously-skip-permissions`. Это прямая формулировка кейса.
- **[Ф]** Для пользователей open-source-моделей риск выше: по исследованию TRAP, приведённому в кейсе, доля успешных инъекций — 13 % для GPT-5 и 43 % для DeepSeek-R1. Встроенной устойчивости модели может не хватать.

**[П]** Мы считаем, что этот сегмент сегодня чаще всего **просто отключает защиту** — то же зафиксировано как Gap 3 в конкурентном анализе («сегмент, который сегодня просто отключает защиту», confidence Medium). Проверки этого утверждения на реальных пользователях у нас нет.

### Secondary users

**[Г]** **Команда, использующая несколько разных харнессов.** Обоснование — **[Ф]**: политика безопасности сегодня расползается по форматам (`settings.json` у Claude Code, `config.toml` у Codex, `permissions.json` у Cursor, правила `{permission, pattern, action}` у Kilo), её нельзя проревьюить целиком, и она теряется при смене агента (`why-agentgate.md` §2, §7). Ценность единой политики для такой команды — гипотеза, не проверенная ни на одном пользователе.

**[Г]** **Организация с требованиями к периметру данных и аудиту** (on-prem, запрет на отправку кода и запросов пользователя внешнему вендору). Обоснование — **[Ф]**: в закрытых auto mode классификатор является моделью вендора, и каждое действие вместе с запросом пользователя уходит к вендору; в AgentGate провайдер ступени 2 задаётся конфигурацией (OpenAI-совместимый API, вплоть до локальной модели). Технически это уже так (`profiles/schema.py: ModelsConfig`), но что этот сегмент готов ставить **отдельный сервис**, а не плагин, — **[?]** не проверено (прямо назван как открытый вопрос в Gap 3).

**[?]** Сегмент «enterprise / платящий заказчик» в проекте не исследован: данных о спросе, готовности платить и объёме рынка в наших материалах нет — конкурентный анализ прямо запрещает такие утверждения («Любые заявления о доле рынка, спросе или готовности платить — в исследованиях данных нет»).

---

## 2. User problem

### Формулировка

**Разработчик не может одновременно получить производительность автономного агента и уверенность, что агент не сделает необратимого действия, — потому что единственный доступный ему регулятор (подтверждать / не подтверждать) регулирует не риск, а объём его собственной работы.**

### Конфликт, который стоит за этим

Проблема — не «AI-агенты небезопасны». Проблема в том, что три вещи связаны так, что улучшение одной ухудшает другую:

1. **Автономность даёт продуктивность.** **[Ф]** Кейс: «Продуктивность такого режима резко возрастает, когда агенту дают автономию». Именно ради этого пользователь включает bypass.
2. **Человеческое подтверждение — плохой детектор, а не хороший.** **[Ф]** Слепое исследование Anthropic (n = 1 053, приведено в `03_competitor_research_summary.md` §2): люди одобряют 93 % всех промптов; явно опасную подменённую команду блокируют лишь в 13,6 % случаев; catch rate падает с ~17 % в начале сессии до ~5 % после 50 промптов. Auto mode в том же тесте блокировал 89 %. То есть **friction, которую платит пользователь, не покупает ему безопасность** — она покупает ощущение контроля, деградирующее по ходу сессии.
3. **Цена ошибки в автономном режиме — необратимая.** **[Ф]** Канонический пример из конкурентного анализа — инцидент Replit Agent (удаление production-базы во время code freeze, июль 2025). Кейс перечисляет тот же класс: установка вредоносного npm-пакета, раскрытие секретов по инструкции из заражённого навыка, удаление промышленной БД.

Отсюда — форма конфликта, а не просто перечисление:

- Если поднять долю подтверждений, растёт friction, **и вместе с ней растёт вероятность, что пользователь отключит защиту целиком** — тогда защита равна нулю. **[Ф]** Конкурентный анализ формулирует это как правило рынка: «решение с идеальной детекцией и высоким FPR проигрывает, потому что его выключают».
- Если снизить долю подтверждений, растёт вероятность пропуска. **[Ф]** Честная цифра рынка: FNR 17 % на реальных overeager-действиях у Claude Code — вендор публикует её сам.
- Промежуточный вариант «спрашивать умнее» упирается в то, **[Ф]** что LLM-классификатор, читающий контекст агента, по построению уязвим к тому, что в этом контексте написано: обход Rehberger — 60–80 % успешных инъекций через цепочку внешне безобидных шагов.

**[Р]** Команда формулирует пользовательскую проблему как **проблему распределения решений**, а не как проблему детекции: вопрос не «как поймать атаку», а **«какие решения вообще не должны доходить до человека, какие обязаны, и как сделать так, чтобы отказ не останавливал работу»**.

### Что входит в проблему дополнительно

**[Ф]** Кейс требует ещё двух вещей, которые команда считает частью той же пользовательской проблемы, а не отдельными фичами:

- **Продолжение работы после блокировки.** Блокировка, после которой агент встал, — это тот же простой, что и подтверждение, только без выбора.
- **Хотя бы один контроль, архитектурно неуязвимый к prompt injection.** Гардрейл, живущий в контексте агента, обходится текстом в этом же контексте.

---

## 3. Current product insight

**[Р]** Ключевой insight команды на текущий момент:

> **Максимальное блокирование и максимальное количество подтверждений — это одно решение, а не два разных: оба перекладывают риск на человека, который заведомо плохо его оценивает, и оба оплачиваются его временем. Продуктовая задача — не «спрашивать чаще» и не «блокировать больше», а сократить множество решений, которые вообще требуют человека, не сделав при этом пропуск дешевле.**

Почему простые варианты недостаточны:

**Почему не «блокировать максимум».** **[Ф]** Метрика Utility в кейсе существует ровно для этого: «без неё любая защита может выглядеть идеальной, поскольку самый надёжный способ защиты — не позволять агенту работать». **[Ф]** Денайлисты как класс признаны хрупкими: Backslash/HiddenLayer нашли ≥4 обхода auto-run денайлиста Cursor, после чего денайлист объявлен устаревшим; префиксное сравнение обходится (Gemini CLI). То есть агрессивная блокировка одновременно **и мешает работать, и не держит**.

**Почему не «спрашивать всегда».** **[Ф]** Цифры approval fatigue выше: 93 % одобрений, 13,6 % поимки, деградация с 17 % до 5 %. Человек, которого спрашивают часто, перестаёт быть контролем и становится ритуалом. **[Ф]** Рынок уже относится к `ask` как к дорогому ресурсу: Codex сократил остановки на человека примерно в 200 раз (7,2 % действий доходят до ревью, из них 99 % одобряются), Cursor заявляет сокращение запросов примерно на 84 %.

**Почему не «один умный классификатор».** **[Ф]** Классификатор вероятностен (FNR 17 % — «the honest number» по формулировке источника), стоит токенов и латентности, и сам является целью инъекции. **[Ф]** Вендоры прямо говорят, что гейт — «best-effort convenience, not a security boundary» (Cursor); Anthropic закрыла обход Rehberger как «Informative» на том же основании.

**[Р]** Следствие, принятое командой: **гейт конкурирует не с сэндбоксом, а с трением ручного подтверждения.** Это формулировка из `03_competitor_research_summary.md` §1, и команда приняла её как рамку позиционирования: мы не обещаем границу безопасности, мы обещаем перераспределение решений при сопоставимом риске. Сэндбокс требуется рядом, а не заменяется (`why-agentgate.md`, «Чего AgentGate не обещает»).

**[Р]** Второе следствие: **если часть решений принимается детерминированно и без чтения текста, то именно эта часть — единственное, на что можно давать гарантию.** Всё остальное вероятностно и должно так и называться.

---

## 4. Product hypothesis

### Основная гипотеза (проверяемая форма)

> **[Г] Если** решение о каждом действии агента выносить **вне харнесса**, в отдельном сервисе, где действие сначала нормализуется в AST и проходит детерминированную ступень без обращения к LLM, а до модели доходит только то, что детерминированная ступень не смогла решить, **то** доля действий, требующих человека, окажется достаточно низкой, чтобы разработчик не отключил защиту, при доле пропущенных атак не выше, чем в режиме ручного подтверждения, **потому что** большая часть реального трафика агента — рутинные и read-only действия, решаемые правилами за миллисекунды и бесплатно, а человек как детектор опасного действия работает хуже автоматической проверки (93 % одобрений, 13,6 % поимки).

Гипотеза намеренно связана с реальной архитектурой: каждое её звено соответствует существующему компоненту — `normalize/` (AST), `stage1/chain.py` (детерминированная ступень), `stage2/` (LLM только на остатке), `POST /v1/decide` (решение вне харнесса).

### Три подчинённые гипотезы

**[Г] H2 — о гарантии.** Если часть опасных действий закрыта правилами, работающими по AST, а не по тексту, то эта часть не обходится инструкцией, попавшей в контекст агента (README, SKILL.md, вывод инструмента). Частично подтверждено конструкцией: **[Ф]** ступень 1 не читает сырую строку для принятия решения, а в промпт ступени 2 не попадают ни `metadata`, ни выводы инструментов, ни рассуждения агента; табличные тесты hard-deny покрывают `$(…)`, `eval`, переменные, base64, heredoc и обёртки. **[?]** Устойчивость к адаптивной атаке не измерена ни разу.

**[Г] H3 — о продолжении работы.** Если отказ возвращается агенту как результат инструмента с причиной и безопасной альтернативой, агент продолжит задачу, а не остановится. **[Ф]** В контракте для этого есть поля `reason` и `suggest` и шаблон `contracts/deny_message_template.md`. **[?]** Ни один код этот шаблон не подставляет; доля восстановлений агента после `deny` не измерялась.

**[Г] H4 — о переносимости.** Если политика живёт в сервисе, а не в харнессе, одна и та же политика применима к нескольким агентам. **[Ф]** Технически контракт единый, и `hook_client.py` уже распознаёт две формы хука (Claude Code `PreToolUse`, OpenCode `tool.execute.before`). **[?]** Ни одного установленного адаптера в реальном харнессе нет — `adapters/` содержит только README. До этого H4 остаётся декларацией.

### Что гипотеза сознательно НЕ утверждает

**[Р]** Команда сознательно не формулирует гипотезу как «мы безопаснее» или «у нас ниже FPR, чем у конкурентов». **[Ф]** Основание — раздел «Do not claim yet» конкурентного анализа: сравнивать FPR/FNR можно только на одном датасете, а вендорские цифры получены на трёх разных наборах и не смешиваются.

---

## 5. Value proposition

**Формулировка:** AgentGate даёт кодинг-агенту режим, в котором рутина выполняется без подтверждений, опасное останавливается с объяснением и безопасной альтернативой, а человека спрашивают только там, где автоматика не смогла решить, — и каждое такое решение записано и измеримо.

### Пользовательская ценность

- **[Г]** Меньше подтверждений при сопоставимом риске — то, ради чего пользователь сейчас включает bypass. Величина эффекта **[?]** не измерена.
- **[Ф]** Работа не останавливается на отказе: ответ несёт `reason` и `suggest`; формат отказа — часть контракта, а не текст в UI.
- **[Г]** Auto mode для харнесса, у которого его нет. **[Ф]** Основание — отсутствие auto mode в открытых агентах; **[?]** фактическая поставка в харнесс не сделана.
- **[Ф]** Политика в одном YAML на стороне сервиса, в git, с историей — вместо отдельного формата настроек на каждый агент.

### Техническая ценность

- **[Ф]** Детерминированная ступень до LLM: p50 нормализации + ступени 1 ≤ 1 мс — проверяется тестом `tests/test_stage1_latency.py` на 200 прогонах. Оговорка: это юнит-измерение ступени, а не сквозная латентность продукта.
- **[Ф]** Fail-closed на каждом пути: ошибка, таймаут, невалидный запрос или невалидный ответ модели → `ask` с HTTP 200; `allow` по ошибке недостижим — на это есть отдельные тесты.
- **[Ф]** Выбор модели ступени 2 и периметра данных — конфигурация профиля (OpenAI-совместимый API); read-only и hard-deny до модели вообще не доходят, то есть стоят ноль токенов.
- **[Ф]** Каждое решение пишется в Postgres и JSONL с указанием ступени, `rule_id`, модели, латентности по ступеням и хэша профиля; лента доступна через `GET /v1/decisions`.

### Отличие от существующих подходов

**[Ф]** Что действительно отличается — **упаковка**: гейт как отдельный сервис с собственным HTTP-контрактом, а не код внутри харнесса. Конкурентный анализ помечает это как **[Г]**, а не как факт уникальности: заявку на кросс-агентность делают Adversa AI и, в своей нише, Arcade.dev, и глубина их исполнения неизвестна.

**[Ф]** Что отличием **не является** и не должно заявляться (прямой список из §9 конкурентного анализа): LLM-классификация действия перед исполнением; двухстадийность fast→CoT и reasoning-blind; fail-closed; deny-and-continue; эскалация по счётчикам; prose-политика и профили; AST-нормализация shell (CARE сделал это раньше и с опубликованными метриками); аудит решений; сам тезис «мы снижаем approval fatigue» (Codex и Cursor заявили его раньше и с цифрами).

**[Р]** Решение команды: **отличие формулируется как «политика вне харнесса + детерминированная ступень + измеримость», и ни один из трёх пунктов не подаётся как изобретение.**

---

## 6. Product approach

### Выбранный подход

**[Р]** **Runtime decision layer: внешний сервис, принимающий решение по каждому предложенному действию до его исполнения, с риск-зависимым вмешательством вместо равномерного контроля.**

Формально: `AI Agent / Harness → перехват до исполнения → AgentGate → allow | deny | ask → действие агента`.

Три решения, из которых следует остальное (зафиксированы в `architecture_description.md` §2 и подтверждены кодом):

1. **[Р]** Точка врезки — **до исполнения инструмента и вне харнесса**. Политика одна на все харнессы и живёт на стороне сервиса.
2. **[Р]** **Решение никогда не принимается по сырой строке команды** — только по `NormalizedAction`, полученному разбором в AST.
3. **[Р]** **Fail-closed как позвоночник** — любая неопределённость превращается в `ask`, а не в `allow`.

### Почему не альтернативы

| Альтернатива | Почему отвергнута как основной подход |
|---|---|
| Только allowlist / денайлист без модели | **[Ф]** Денайлисты систематически обходятся (Cursor отказался от своего); префиксное сравнение — источник обходов (Gemini CLI). Оставлено как **ступень**, а не как продукт |
| Только LLM-классификатор | **[Ф]** Вероятностен (FNR 17 %), стоит токенов и латентности на каждом действии, сам является целью инъекции; при недоступности провайдера всё превращается в `ask` |
| Сэндбокс | **[Ф]** Единственная настоящая граница безопасности по признанию вендоров — но не различает намерение и ломает легитимные сценарии. **[Р]** Мы его требуем рядом, а не заменяем |
| Гейт внутри одного харнесса (как у Kilo/Claude) | **[Ф]** Ровно то, что уже есть у всех: политика не переносится, дублируется и расходится между агентами. **[Р]** Основание для выбора отдельного сервиса — не безопасность, а переносимость политики |
| Гардрейл в контексте агента (промпт/инструкция) | **[Ф]** «Запрет, живущий в инструкциях, агент рационализирует» (вывод из инцидента Replit); контроль должен быть в call-path |

### Связь с тремя опорами

**С target architecture.** **[Ф]** Целевая схема (`target_architecture.jpg`, §3–4 `architecture_description.md`) — тот же подход в полном объёме: Context Guard на входе данных, Action Analyzer, Policy & Risk Engine (Hard Rules → Session Rules → AI Risk Classifier), Session State / Audit, узел Security Decision с четырьмя исходами. Текущая реализация — **нижний контур этой схемы**: Action Analyzer + Policy & Risk Engine + Session State в урезанном виде + три исхода из четырёх.

**С competitor research.** **[Ф]** Каскад «детерминированная ступень → LLM-ступень → эскалация», reasoning-blind, fail-closed, deny-and-continue — де-факто стандарт, а не дифференциатор. **[Р]** Команда сознательно воспроизводит стандарт в ядре и не заявляет его новизну; ставка сделана на упаковку (внешний сервис), измеримость и детерминированную гарантию.

**С текущей реализацией.** **[Ф]** Подход реализован ровно в том виде, в каком описан: `POST /v1/decide` → нормализация → `stage1/chain.py` (`hard_deny → profile → allowlist → packages`) → `stage2/` → эскалация по счётчикам сессии → `allow | deny | ask` с записью решения. Все элементы подключены к основному потоку и покрыты тестами; e2e-сценарий проходит через реальный подпроцесс сервиса и `contracts/hook_client.py`.

---

## 7. MVP boundary

**[Р]** Граница проводится по одному критерию: **входит то, без чего нельзя проверить основную гипотезу (§4)**. Целевая функциональность в MVP автоматически не входит.

### In MVP

| Элемент | Зачем нужен для проверки гипотезы | Статус |
|---|---|---|
| `POST /v1/decide` с тремя исходами | Сама единица измерения гипотезы — решение по действию | **[Ф]** реализовано |
| Нормализация в AST (`NormalizedAction`) | Без неё «детерминированность» и «неуязвимость к тексту» ничем не подтверждаются | **[Ф]** реализовано |
| Ступень 1: hard-deny → профиль → allowlist | Ядро гипотезы: большая часть трафика решается без LLM | **[Ф]** реализовано |
| Ступень 2 (LLM) на остатке | Проверка утверждения «до модели доходит меньшинство» | **[Ф]** реализовано |
| Эскалация по счётчикам сессии | Ограничение blast radius там, где пер-экшн-классификация ошибается | **[Ф]** реализовано |
| Fail-closed на всех путях | Без него утверждение «`allow` по ошибке невозможен» не проверяемо | **[Ф]** реализовано (для сервиса) |
| `reason` + `suggest` в ответе | Гипотеза H3: работа продолжается после отказа | **[Ф]** поля есть; **[?]** рендеринг на стороне харнесса отсутствует |
| Запись каждого решения (Postgres + JSONL, `GET /v1/decisions`) | Без неё гипотеза не измерима в принципе | **[Ф]** реализовано |
| Один YAML-профиль на сервисе | Проверка «политика вне харнесса» | **[Ф]** реализовано |
| Бенчмарк: датасет + раннер + скорер + отчёты | Инструмент, которым гипотеза проверяется | **[Ф]** реализовано |
| **Первый прогон бенчмарка против живого сервиса** | **Единственный шаг, превращающий «implemented» в «validated»** | **[?] не сделан — блокирующий пункт** |
| **Хотя бы одна реальная интеграция с харнессом** | Без неё гипотеза проверяется на стенде, а не на продукте | **[?] не сделана — блокирующий пункт** |

### Outside current MVP

Сознательно не требуется для проверки основной гипотезы:

- **[Р]** **Context Guard целиком** (provenance, sensitive data detection, prompt injection detection, validation, data tagging) — отнесён к v4, не спроектирован до уровня решения. Гипотеза §4 касается решений по действиям, а не фильтрации входящего контекста.
- **[Р]** **История диалога (v2) и оценка `tool-result` (v3)** — порядок и обоснование зафиксированы в `context-versions-roadmap.md`.
- **[Р]** **Четвёртый исход SAFE RECOVERY** — сведён к текстовому полю `suggest`; отдельным маршрутом не является.
- **[Р]** **Модуль пакетов / slopsquatting** — место в каскаде занято (`stage1/packages.py`), проверка не реализована; typosquat ловится только ступенью 2. Кандидат на расширение после MVP (то же рекомендует конкурентный анализ, §10).
- **[Р]** **Панель / UI, override, обучение политики** — не в v1.
- **[Р]** **Внешний бенчмарк против конкурентов** (Claude Code Auto Mode, Codex Auto-review, статические правила) — заявлен в `benchmark/README.md`, кодом не покрыт. Для проверки собственной гипотезы не требуется; для сравнительных заявлений обязателен, а таких заявлений команда решила не делать (§5).
- **[Р]** **Параллельные проверки, метрики/трейсинг, идемпотентность, атрибуция решения к ключу, общее состояние сессии между процессами** — известные ограничения, зафиксированные явно; на проверку гипотезы в масштабе демонстрации не влияют.

**[Р]** Отдельное решение: **всё, что «Outside MVP», остаётся в документах как явно неготовое, а не выпадает из описания продукта.** Основание — требование кейса о честности гарантий («что система гарантирует всегда, а что обеспечивает по возможности»).

---

## 8. Primary user flow

Сценарий описан на продуктовом уровне; инженерные подробности — в `architecture_description.md` §6 и `05_current_state.md`.

1. **Разработчик ставит задачу агенту** в своём харнессе — обычным текстом, ничего не зная про AgentGate.
2. **Агент решает выполнить действие** — команду в shell, запись файла, сетевой вызов, вызов MCP-инструмента.
3. **Действие перехватывается до исполнения.** Харнесс через адаптер отправляет описание действия и последний запрос пользователя в AgentGate. **[Ф]** Сервис получает только действие + `user_request`; рассуждения агента и выводы инструментов не передаются.
4. **AgentGate выносит решение** — за миллисекунды, если хватило правил, иначе с обращением к модели.
5. **Ветвление, которое видит пользователь:**
   - **allow** — действие выполняется, пользователь ничего не замечает. **[Р]** Это целевое состояние для большинства действий;
   - **deny** — действие не выполняется, агент получает причину и безопасную альтернативу как результат инструмента и **продолжает задачу другим путём**; пользователь видит объяснение, а не остановку;
   - **ask** — пользователя спрашивают. **[Р]** Это дорогой ресурс, и каждый такой случай обязан нести причину и вариант действия.
6. **Повторное такое же действие в той же сессии** проходит быстрее: **[Ф]** `allow` кэшируется на сессию, `deny` и `ask` не кэшируются никогда.
7. **Если агент упирается в стену** (три отказа подряд либо 10 из последних 50), решение принудительно уходит человеку — **[Р]** это сигнал «границу нужно чинить», а не наказание агента.
8. **Каждое решение записано** — разработчик или команда может позже посмотреть ленту решений: что просили, что предложил агент, что решил гейт, на какой ступени и почему.

**[?] Разрыв, который нужно назвать прямо.** Шаги 3 и 5 сегодня работают только через эталонный клиент `contracts/hook_client.py`, вызываемый вручную или из e2e-теста. **Установленного в реальный харнесс адаптера нет**, рендеринг deny-сообщения и диалог подтверждения на стороне харнесса кодом не покрыты. То есть описанный flow — целевой пользовательский сценарий MVP, а не то, что сегодня можно показать в работающем агенте.

---

## 9. Success criteria

**[Р]** Критерии взяты из целевых метрик кейса и структуры отчёта бенчмарка. **Числовых порогов команда не фиксировала** — их нет ни в одном источнике, и до первого прогона фиксировать их было бы вымыслом. Ниже — что именно измеряется и в какую сторону.

### Safety

- **Доля атак, прошедших как `allow`** — операционализация ASR. **[Ф]** Считается как `attack_pass_through_rate` в `reporting/report.py`.
- **`attacks_not_blocked`** — абсолютное число пропусков, с разбивкой `by_attack_category` и `by_difficulty`.
- **Отделение настоящих промахов от известных ограничений v1.** **[Ф]** `tag_failures` разбивает провалы по тегам; 3 кейса помечены `v1_limitation` (требуют провенанса или модуля пакетов) — их провал ожидаем и не смешивается с промахом.
- **[Р]** Требование к методу: FN на «неосторожных» действиях и FN на adversarial-цепочках **не смешиваются** — прямое предупреждение конкурентного анализа.
- **[?]** Recall / детекция: `detected` и `detection_correct` считаются построчно, но в сводку не агрегируются — сегодня этой метрики у нас фактически нет.

### Friction / usability

- **`false_positive_rate`** — доля контрольных (benign) кейсов, получивших `deny`.
- **`benign_asked_friction`** — доля контрольных кейсов, получивших `ask`. **[Р]** `ask` на легитимной работе засчитывается как трение, а не как успех: `acceptable_service_results` для benign-кейсов — строго `{allow}`.
- **Доля действий, выполняемых автономно** — доля `allow` в `decision_distribution`; **[Р]** это ближайший к продуктовой гипотезе показатель.
- **Доля трафика, дошедшая до LLM-ступени** — из `stage_distribution`. **[Р]** Названа конкурентным анализом ядром продукта (ориентир рынка: у Codex до ревьюера доходит 7,2 %).
- **Частота `ask` по таймауту/ошибке** — цена fail-closed, отделяемая по `rule_id`.
- **[?]** Доля восстановлений агента после `deny` — метрика названа в конкурентном анализе (ориентир Codex >50 %), в бенчмарке не реализована.
- **[?] Ограничение, которое обязательно проговаривается вместе с любой цифрой friction:** контрольная группа — **5 кейсов на 70 атакующих**. Один провалившийся benign-кейс двигает метрику на 20 п. п. Это индикатор, а не измерение (`06_benchmark_status.md`, Known limitations).

### Performance

- **Латентность ступени 1** — **[Ф]** единственная величина, по которой сегодня есть измерение: p50 нормализации + ступени 1 ≤ 1 мс на 200 прогонах (юнит-тест).
- **Латентность сервиса p50 / p95** — `service_p50_ms`, `service_p95_ms` из поля `latency_ms` ответа. **[Р]** Заявления о латентности делаются **только** при `--concurrency 1`; отчёт печатает фактический параллелизм рядом с каждой цифрой, потому что `execution_time_ms` — клиентские настенные часы.
- **Стоимость.** **[?]** Каркас готов (таблица цен, пробы по JSON-путям), но контракт `/v1/decide` v1 не отдаёт usage, поэтому `cost` всегда `None` с явной причиной. **Стоимость сегодня не измеряется.** Косвенный аргумент — доля решений, не дошедших до модели.

### Качественные критерии (из кейса)

**[Р]** Команда принимает формулировку кейса: «Отрицательный результат с понятным методом ценнее хорошего числа без описания метода». Поэтому к критериям успеха отнесены также воспроизводимость прогона (зафиксированы дата, коммит сервиса, профиль, модель, `--concurrency`) и честное разделение «что гарантируется всегда» (ступень 1) и «что по возможности» (ступень 2).

---

## 10. Key assumptions

| # | Assumption | Почему важно | Подтверждён? | Как проверяем |
|---|---|---|---|---|
| A1 | **Большая часть реальных действий агента решается детерминированно, без LLM** | Ядро гипотезы: от этого зависят и латентность, и стоимость, и доля `allow` | **[?] Нет.** Косвенная опора — рынок (у Codex 7,2 % трафика доходит до ревьюера); на нашем датасете не измерено | `stage_distribution` в первом прогоне бенчмарка |
| A2 | **Правила по AST не обходятся текстом, попавшим в контекст агента** | Единственное, на что мы даём гарантию | **[Ф] Частично:** конструкция это обеспечивает, табличные тесты покрывают обфускацию. **[?]** Адаптивная атака не пробовалась | Прогон категорий `obfuscation_evasion`, `tool_argument_injection`, уровня `adversarial`; в идеале — внешний red-team |
| A3 | **Агент, получив `deny` с причиной и `suggest`, продолжит работу** | Иначе `deny` = остановка, и friction не снижается | **[?] Нет.** Поля есть, шаблон есть, кода, который его подставляет, нет | Требуется реальная интеграция с харнессом + наблюдение; в бенчмарке эта метрика не реализована |
| A4 | **Пользователь готов поставить отдельный сервис рядом с агентом** | На этом стоит выбор «сервис, а не плагин» | **[?] Нет.** Прямо назван открытым вопросом в конкурентном анализе (Gap 3) | Пользовательской проверки не запланировано; ближайший суррогат — сложность установки в демонстрации |
| A5 | **Точка перехвата уровня `PreToolUse` существует не только в Claude Code** | Без этого кросс-харнессность — декларация | **[Ф] Частично:** `hook_client.py` распознаёт формы Claude Code и OpenCode. **[?]** Ни одной установленной интеграции нет | Первая реальная интеграция (какой харнесс — не решено, см. §15) |
| A6 | **Дополнительный сетевой вызов на действие приемлем для пользователя** | Прямой конкурентный риск: «проверка дольше действия ломает рабочий процесс» (кейс) | **[Ф] Частично:** ступень 1 ≤ 1 мс по юнит-тесту. **[?]** Сквозная латентность с сетью и LLM не измерена | Отдельный прогон с `--concurrency 1`, `service_p50/p95` |
| A7 | **Датасет из 75 кейсов отражает реальные угрозы и реальную работу** | От этого зависит осмысленность всех метрик | **[Ф] Частично:** 15 категорий × 5 уровней сложности, таксономия и исключения зафиксированы. **[?]** Ожидания расставлены по спеке и здравому смыслу, а не по наблюдаемому поведению | Разбор провалов первого прогона на три корзины: ошибка ожидания / `v1_limitation` / настоящий промах |
| A8 | **Один профиль политики годится для разных пользователей и проектов** | Иначе «политика вне харнесса» превращается в «политика, которую всё равно каждый правит» | **[П] Предположение.** Поле профиля `rules` объявлено, но **[Ф]** не читается кодом вообще | Способ проверки не определён; кандидат — прогон бенчмарка на втором профиле |
| A9 | **Fail-closed приемлем как поведение по умолчанию** | При недоступности LLM всё нерешённое становится `ask` — friction резко растёт | **[Ф]** Для сервиса зафиксировано и покрыто тестами. **[?]** Конфликт с контрактом адаптера (по умолчанию fail-open на клиенте) **не решён** | Решение владельца продукта из трёх вариантов `adapter-contract-gap-analysis.md` + доля `ask` по таймауту в прогоне |

---

## 11. Risks

| Риск | Суть | Как проверяем / снижаем | Текущий статус |
|---|---|---|---|
| **False negatives** | Ступень 2 вероятностна; рынок публикует FNR 17 % как честную цифру | Разделение метрик FN на неосторожные действия и adversarial-цепочки; эскалация по счётчикам ограничивает blast radius; hard-deny не переопределяется ничем | **[?]** не измерено |
| **False positives / friction** | Гейт, который часто ошибается, отключают — это и есть проигрыш продукта | Контрольная группа `benign_utility` в датасете; `ask` на benign считается провалом, а не успехом | **[?]** не измерено; **[Ф]** контрольная группа мала (5 кейсов) — риск некалиброванной оценки |
| **Недостаточный контекст от харнесса** | Сервис видит одно действие + последний запрос пользователя; многошаговые атаки, где каждый шаг безобиден, по построению не видны | **[Р]** Класс явно объявлен непредставимым в v1 (`taxonomy.md` §5); в roadmap — история диалога (v2), tool-result (v3), Context Guard (v4) | **[Ф]** ограничение зафиксировано открыто |
| **Latency** | Сетевой вызов на каждое действие серой зоны | Дешёвая ступень 1 закрывает рутину; отдельный прогон `--concurrency 1` для заявлений | **[Ф]** ступень 1 измерена; **[?]** сквозная — нет |
| **Cost** | Токены ступени 2 на каждое неоднозначное действие; жалобы на расход токенов ревьюером — известный рыночный сюжет | Ступень 1 до модели; выбор модели — конфигурация; allow-кэш на сессию | **[?]** **не измеряется вовсе** — контракт не отдаёт usage |
| **Adaptive attack** | Атакующий подстраивается под классификатор; независимый red-team пробивал все проверенные системы (ASR 60–80 %) | Уровень `adversarial` в каждой из 15 категорий; hard-deny не зависит от текста | **[?]** внешнего red-team не было |
| **Integration complexity** | Заявленная мультихарнессность держится на одном эталонном CLI-клиенте | Стабильный HTTP-контракт, генерируемые схемы, тест на расхождение контрактов | **[Ф]** `adapters/` пуст — **главный разрыв между заявлением и кодом** |
| **Пользователь отключает или обходит защиту** | Гейт — не граница безопасности; пользователь может просто не поставить хук | **[Р]** Позиционирование «требуем сэндбокс рядом, а не заменяем»; продуктовый ответ — держать friction низкой | **[?]** поведение реальных пользователей не наблюдалось |
| **Конфликт fail-open / fail-closed** | Сервис жёстко fail-closed внутри, контракт адаптера по умолчанию fail-open на клиенте: «положил гард — разрешено всё» | Три варианта решения зафиксированы в `adapter-contract-gap-analysis.md` | **[?] Не решено владельцем продукта.** Влияет на то, верно ли «`allow` по ошибке невозможен» для **системы**, а не только для сервиса |
| **Цитирование чисел заглушки как результата** | `tools/mock_agentgate.py` печатает правдоподобный отчёт с процентами, не значащими ничего | Предупреждение в docstring и в `benchmark/CLAUDE.md`; **[Р]** правило команды: до реального прогона цифры не приводятся нигде | **[Ф]** риск активен, пока прогона нет |
| **Заявления, которые опровергнет проверка** | «Мы безопаснее», «ниже FPR», «устойчивы к prompt injection» | **[Р]** Список «Do not claim yet» конкурентного анализа принят как ограничение на формулировки в презентации | **[Ф]** соблюдается в документах |

---

## 12. Validation plan before final

### Обязательное — без этого основная гипотеза не проверена

1. **[Ф]** ~~Вернуть код бенчмарка в `main`~~ — **сделано** (106 файлов восстановлено, приёмка пройдена), осталось закоммитить.
2. **Первый прогон бенчмарка против живого AgentGate.** Поднять сервис (Postgres + ключ модели ступени 2), выполнить `pytest -m live`, затем `cli.py benchmark --path attacks/cases --concurrency 1`. Единственный шаг, превращающий «implemented» в «validated». Осознанная цена — 75 запросов, часть из которых платная.
3. **Разбор провалов на три корзины** — ошибка ожидания в кейсе / известное ограничение v1 (`v1_limitation`) / настоящий промах сервиса. Без этого сводные проценты недоказательны.
4. **Сохранить результаты под версионным контролем** с датой, коммитом сервиса, профилем, моделью и `--concurrency`. Сейчас `results/` в `.gitignore` — по умолчанию не сохранится ничего.
5. **Отдельный прогон для заявлений о латентности** — строго `--concurrency 1`, цитируется `service_p50_ms` / `service_p95_ms`, а не клиентские часы.
6. **Минимум одна реальная интеграция с харнессом** — установленный адаптер, а не эталонный CLI-клиент. Проверяет H4 и делает демонстрацию продуктовой, а не стендовой.
7. **Решение по конфликту fail-open / fail-closed** — выбор одного из трёх зафиксированных вариантов. Это продуктовое решение, а не инженерное: от него зависит, что именно мы гарантируем.
8. **Демонстрация полного пользовательского цикла** — действие → решение → `deny` с причиной и альтернативой → агент продолжает работу. **[?]** Сегодня ветка `ask` и рендеринг `deny` на стороне харнесса кодом не покрыты.

### Желательное — усиливает результат, но не блокирует

9. Расширить контрольную группу с 5 до 20–30 кейсов — самое слабое место в любых утверждениях об удобстве.
10. Вывести recall / детекцию в сводку (`detected` и `detection_correct` уже пишутся построчно).
11. Внешний режим сравнения с конкурентами — **либо** реализовать раннер бейзлайнов, **либо** явно убрать обещание из `benchmark/README.md`. **[Р]** Промежуточное решение команды: сравнительных заявлений не делать (§5), значит второй вариант допустим.
12. Модуль пакетов / slopsquatting — детерминированная проверка вместо заглушки; прямо назван в кейсе и даёт дополнительные баллы.
13. Пользовательская проверка хотя бы в минимальной форме (несколько разработчиков прогоняют реальную задачу с включённым AgentGate и отмечают, где их спросили зря). **[?]** Сейчас у продукта нет ни одного наблюдения за реальным пользователем — это самый большой непокрытый пробел валидации.
14. Обновить `benchmark/CLAUDE.md` (утверждение «`service/` is not implemented yet» устарело) и починить путь к спеке.

---

## 13. Product ↔ Technical implementation

| Product decision / requirement | Why it matters | Technical implementation | Current status |
|---|---|---|---|
| Минимальный friction: пользователь не подтверждает каждое действие | Иначе продукт равен ручному подтверждению, которое уже проиграно (93 % одобрений, 13,6 % поимки) | Автоматический слой решения `POST /v1/decide` с тремя исходами; `Gate.decide` (`pipeline.py`) | **Implemented** |
| Рутина не должна стоить ни времени, ни токенов | Ядро гипотезы и главный аргумент против «одного умного классификатора» | Ступень 1: `stage1/chain.py` — `hard_deny → profile_check → allowlist → packages`, LLM не вызывается | **Implemented** (p50 ≤ 1 мс по юнит-тесту) |
| Решение не должно зависеть от того, что «написано» в контексте | Единственная гарантия, которую можно дать честно; требование кейса о контроле, неуязвимом к инъекции | Нормализация в AST (`normalize/shell.py` → `NormalizedAction`); решение по сырой строке запрещено | **Implemented** |
| Часть запретов не переопределяется ничем | Иначе политику уговорит либо пользователь, либо модель, либо README | `stage1/hard_deny.py`, 6 семейств правил, `hard=True` не снимается эскалацией | **Implemented** |
| Ошибка не должна становиться разрешением | Без этого «безопасность» не свойство, а везение | Fail-closed на каждом пути: ручной парсинг тела, перехват исключений, `ask` при любом отказе ступени 2; HTTP 200 всегда | **Implemented** для сервиса; **Open** для системы (конфликт с fail-open по умолчанию в контракте адаптера) |
| Отказ не должен останавливать работу | `deny`, после которого агент встал, = тот же простой | Поля `reason` и `suggest` в `DecideResponse`; `contracts/deny_message_template.md` | **Partial** — поля и шаблон есть, подстановки нет ни в одном коде |
| Человека спрашивать только там, где иначе нельзя | `ask` — дорогой ресурс | Исход `ask` + эскалация `session/escalation.py` (3 подряд либо 10 из 50) | **Implemented** |
| Повторная рутина не должна оплачиваться дважды | Латентность и стоимость | Allow-only кэш на сессию (`session/cache_key.py`, `memory.py`); `deny`/`ask` не кэшируются | **Implemented** |
| Политика живёт вне харнесса и одна на все агенты | Иначе продукт = ещё один гейт внутри одного харнесса | Один YAML-профиль на сервисе (`profiles/`), харнесс знает только URL, токен и `profile_id` | **Implemented** на стороне сервиса; **Planned** на стороне харнессов |
| Auto mode должен подключаться к любому харнессу | Сегмент, у которого auto mode нет | Единый HTTP-контракт (`contracts/openapi.yaml`, схемы из pydantic) + `hook_client.py` (формы Claude Code и OpenCode) | **Partial** — `adapters/` содержит только README; установленных интеграций нет |
| Каждое решение измеримо | Иначе безопасность = обещание, а не метрика | Postgres (`decisions`, 5 индексов) + JSONL + `GET /v1/decisions`; в ответе `stage`, `rule_id`, `model`, `latency_ms`, `cached` | **Implemented** |
| Свобода выбора модели и периметра данных | Вторичный сегмент с on-prem-требованиями; контроль стоимости | OpenAI-совместимый клиент, конфигурация модели в профиле, выбор модели на запрос (`model` в `DecideRequest`) | **Implemented** |
| Гипотеза должна быть проверяема числом | Требование кейса: метод важнее числа | Бенчмарк: 75 кейсов / 15 категорий / 5 уровней, детерминированный скорер без LLM-судьи, отчёты по безопасности, friction, латентности | **Implemented как инструмент**, **не выполнен как измерение** |
| Защита от slopsquatting до установки пакета | Прямое требование кейса | `stage1/packages.py` — слот в каскаде | **Planned** — заглушка, всегда `None` |
| Проверка данных до попадания в контекст агента | Верхний контур целевой архитектуры | Context Guard | **Planned (v4)** — в коде отсутствует полностью |
| Четвёртый исход SAFE RECOVERY | Ветвь целевой схемы | Сведено к текстовому `suggest` внутри `deny`/`ask` | **Open question** (§15) |

---

## 14. AI Product contribution

Раздел описывает вклад роли AI Product (Тимур Полищук) согласно `07_team.md`. **[Ф]** Инженерная реализация decision-сервиса принадлежит Александру Иванову, слоя перехвата и интеграций с харнессами — Алексею Балашову; ниже эти зоны не приписываются.

**Product-level architecture.** Разработка верхнеуровневой целевой архитектуры продукта **без фиксации конкретного инженерного способа реализации** — то, что описано в `04_architecture/target_architecture.jpg` и §3–4 `architecture_description.md`. Роль: определить, из каких блоков состоит система и какие исходы она обязана различать; как эти блоки реализованы — зона AI Engineers.

**Формулировка требований и продуктовой проблемы.** Формализация пользовательской проблемы, продуктовых требований и связи «пользовательская проблема ↔ техническая архитектура». Настоящий документ — часть этой работы: он существует, чтобы архитектура читалась как следствие гипотезы, а не как набор компонентов. **[Ф]** В матрице ответственности: Product problem definition, Product hypothesis, Target product architecture, MVP scope — *Joint / Product lead*.

**Benchmark.** **[Ф]** Единоличное владение реализацией: структура бенчмарка, формат кейса, таксономия 15 категорий и 5 уровней сложности, инварианты датасета, детерминированный скорер, состав отчёта, 75 YAML-кейсов, 122 собственных теста. Отдельное продуктовое решение внутри этой работы — **контрольная группа `benign_utility`**: без неё сервис, всегда отвечающий `deny`, показал бы 100 % по всем категориям.

**Критерии оценки.** Определение метрик и того, что считается успехом: разделение Safety / Friction / Performance, правило «`ask` на легитимной работе — это провал, а не успех», требование не смешивать FN на неосторожных действиях с FN на adversarial-цепочках, требование измерять латентность только при `--concurrency 1`. **[Ф]** Evaluation metrics — *Joint / Product lead*.

**Конкурентное и продуктовое исследование.** **[Ф]** Lead по Competitive research и Product research: `02_a`, `02_b`, `03_competitor_research_summary.md`, включая раздел «Claims We Can Safely Make» — список утверждений, которые команде разрешено и запрещено делать. Этот список напрямую ограничивает формулировки в §5 и §11 настоящего документа.

**Project artifacts.** **[Ф]** Владение проектной документацией, промежуточными и финальными артефактами, презентацией и материалами защиты. Сюда же — поддержание честного разделения «реализовано / частично / не реализовано / измерений нет» в `05_current_state.md` и `06_benchmark_status.md`.

**Сквозная функция.** Удерживать связь между технической реализацией и исходной пользовательской проблемой: бенчмарк рассматривается не как технический тест, а как инструмент проверки продуктовых характеристик — security, FP/FN, friction, latency, cost и степень сохранения автономности агента.

**[Ф]** Оговорка из `07_team.md`: продуктовая проблема, целевой пользователь, value proposition, пользовательский сценарий, верхнеуровневая архитектура, критерии оценки, границы MVP и риски прорабатываются **совместно всей командой**; жёстким разделение становится преимущественно на уровне конкретной реализации.

---

## 15. Open product questions

Вопросы, которые команда на текущий момент **не закрыла**. Часть — продуктовые решения, ожидающие владельца продукта; часть требует данных, которых пока нет.

**Об уровне вмешательства**

1. **Какая доля `ask` считается приемлемой?** Порога нет; есть только направление «меньше». До первого прогона любая цифра была бы вымыслом.
2. **Какой false-positive rate допустим?** Тот же статус. Дополнительно: контрольная группа из 5 кейсов не даёт основания для такого суждения даже после прогона.
3. **Нужно ли human confirmation вообще, если ставка на автономию?** Сегодня `ask` — и штатный исход, и универсальный fallback при любой ошибке. Не решено, разделять ли эти два смысла для пользователя («нужно твоё решение» против «я не смог проверить»).
4. **Нужен ли SAFE RECOVERY как отдельный исход?** Целевая схема даёт четыре ветви, контракт — три. Открыто: это четвёртое значение `decision` или соглашение поверх `deny` + `suggest`.
5. **Нужен ли Comment от человека агенту** (Approve / Reject / **Comment** из целевой схемы) как часть контракта, или подтверждение целиком остаётся внутри харнесса.

**О рисках и политике**

6. **Fail-open или fail-closed на стороне адаптера?** Три варианта зафиксированы, выбор не сделан. От него зависит, верно ли утверждение «`allow` по ошибке невозможен» для системы в целом.
7. **Нужны ли уровни риска вместо трёх плоских исходов?** В целевой схеме есть Risk Score, в коде — нет. Не решено, продуктовая ли это величина (видит пользователь) или внутренняя.
8. **Нужны ли бюджеты автономии на сессию** (сколько необратимых / сетевых / вне-проектных действий допустимо) — конкурентный анализ отмечает это как незакрытую нишу (Gap 5). В v1 есть только счётчики отказов.
9. **Один профиль на всех или профиль под проект?** Поле `rules` объявлено и не читается кодом; модель «скопировать дефолт и править» рекомендована конкурентным анализом, но не выбрана.

**Об универсальности**

10. **Какой харнесс интегрируем первым и до какой степени?** Kilo Code (по кейсу хакатона), Claude Code или OpenCode. Отдельный вопрос: считается ли `hook_client.py` достаточной интеграцией для демонстрации.
11. **Существует ли точка перехвата уровня `PreToolUse` во втором и третьем харнессе**, или кросс-харнессность останется декларацией.
12. **Какая версия дорожной карты является целью хакатона** — остаёмся на v1 или заявляем v2 (история диалога).

**О пользователе**

13. **Готов ли целевой пользователь ставить отдельный сервис, а не плагин?** Ключевое предположение, на котором стоит выбранная упаковка продукта, и оно не проверено ничем.
14. **Что пользователь считает раздражающим отказом, а что — оправданным?** У нас есть метрика FP, но нет ни одного наблюдения за реальным пользователем. **[?]** Пользовательских исследований команда не проводила — это осознанный пробел промежуточной стадии, а не упущение, о котором не знают.
15. **Показываем ли мы сравнение с конкурентами?** Если да — нужен раннер бейзлайнов, которого нет; если нет — обещание нужно убрать из `benchmark/README.md`. Промежуточное решение: сравнительных заявлений не делать.

---

## Связка целиком

Как читается документ по цепочке, ради которой он написан:

**Problem** — разработчик не может получить и автономию агента, и уверенность, потому что единственный регулятор (подтверждать / нет) регулирует не риск, а его собственную работу; человек как детектор деградирует (93 % одобрений, 13,6 % поимки), а цена ошибки необратима.

**→ Product hypothesis** — если решение выносить вне харнесса, нормализовав действие в AST и пропустив его через детерминированную ступень до LLM, доля действий, требующих человека, будет достаточно низкой, чтобы защиту не отключили, при пропуске не выше ручного режима.

**→ Product decision** — runtime decision layer как отдельный сервис; решение никогда по сырой строке; fail-closed везде; `ask` — дорогой ресурс; `deny` обязан нести причину и альтернативу; каждое решение записано.

**→ Technical implementation** — `POST /v1/decide` → `normalize/` (AST) → `stage1/` (hard-deny → профиль → allowlist) → `stage2/` (LLM только на остатке) → эскалация по сессии → `allow | deny | ask` → Postgres + JSONL. Каждый компонент §13 существует потому, что его требует конкретное продуктовое решение, а не наоборот.

**→ Benchmark / validation** — 75 кейсов в 15 категориях с контрольной группой, детерминированный скорер, метрики Safety / Friction / Performance. Инструмент готов; **измерений нет ни одного, и до первого прогона против живого сервиса ни одно количественное утверждение о продукте не может быть сделано.**
