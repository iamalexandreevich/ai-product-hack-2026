# AgentGate — дорожная карта версий (v1 → v5)

Дата: 3 сентября 2026. Статус: **принято владельцем продукта, порядок зафиксирован.**

Основная ось развития: сколько контекста агента сервис видит и берётся оценивать. По ней идут v1→v4, и каждая включает предыдущую.

**v5 стоит на другой оси** — независимость от провайдера модели: сервис общается с любым OpenAI-совместимым эндпоинтом по тройке параметров и сам моделей не запускает. Она ортогональна первой: её можно делать до, после или параллельно любой из v2–v4, и она ничего не меняет в объёме контекста. Номер даёт владелец продукта, чтобы вещь не потерялась; порядковой зависимости от v4 у неё нет.

| Версия | Что видит сервис | Спека | Статус |
|---|---|---|---|
| **v1** | последний tool-call + последнее сообщение пользователя | `2026-09-03-agentgate-v1-design.md` | реализовано |
| **v2** | v1 + предшествующий диалог: `assistant` + `toolcall` + `toolresult` + `human` | `2026-09-04-agentgate-v2-design.md` (постановка — `v2-dialogue-context.md`) | **реализовано** |
| **v3** | v2 + оценка самих `tool-result` | `2026-09-04-agentgate-v3-rules-and-inspect-design.md` (постановка — `v3-tool-result-evaluation.md`) | **реализовано** |
| **v4** | v3 + семантический Context Guard: модель возвращает спаны, сервер маскирует; плюс маскирование секретов шире `.env` | `2026-09-05-agentgate-v4-context-guard-design.md` (различение с экранированием — `v4-context-guard.md`) | **спроектировано** |
| **v5** | *(другая ось)* модель как тройка `base_url` / `api_key` / `model_name`; ризонинг выключен, SO включён | `v5-model-binding.md` | спроектировать |

## Где мы сейчас: v2 реализован

- `DecideRequest` (`service/agentgate/api/schemas.py`) несёт `history` и `protocol` вдобавок к `tool`, `raw`, `args`, `user_request`.
- `build_user_message` (`service/agentgate/classify/prompt.py`) рендерит `[TASK]`, `[HISTORY]`, `[ACTION]`, `[FLAGS]`, `[STAGE1]`.
- Закрытый список содержимого промпта включает `[HISTORY]`.

Граница v2 задана спекой сознательно, а не является недоделкой: `/v1/inspect` и оценка `toolresult` в неё не входят и приезжают на v3, семантическое маскирование промпт-инъекций (Context Guard) — на v4.

## v3 реализован

- `DecideRequest` несёт `rules` (детерминированные `allow`/`ask`/`deny` пользователя) и `call_id`; ступень 1 применяет их на трёх позициях цепочки `STAGE1`.
- `POST /v1/inspect` судит один результат инструмента: детекторы → построчная маска или `drop` → классификатор ступени 2 по флагу. Три вердикта `pass | mask | drop`, fail-closed — `drop`, кэш по содержимому, `?kind=` в ленте.
- Отчёт: `docs/reports/task-22-v3-rules-and-inspect.md`.

Граница v3 тоже сознательна. На v4 остаётся ровно семантика: классификатор возвращает **спаны** текста, а маскирует их сервер (сейчас модель отвечает одной буквой, а маска — построчная замена), и маскирование секретов шире `.env`. Перефразированные инъекции, которые не ловит ни одна регулярка, — тоже v4.

## v5 — модель как тройка параметров

Полная спека: `v5-model-binding.md`. Кратко, чтобы карта читалась без перехода:

Ступень 2 привязывается к модели адресом эндпоинта, ключом и именем модели — и ничем больше. Три требования:

1. **Ризонинг выключён, structured output включён.** Не предпочтение, а условие работоспособности: у ступени 2 таймаут порядка трёх секунд, а любой таймаут по fail-closed становится `ask`, то есть вопросом человеку. Рассуждающая модель без отключения превращает каждое серое действие во фрикцию — ровно в ту метрику, которую продукт минимизирует. Портируемого параметра для этого не существует: OpenAI, OpenRouter, Anthropic и сборки на vLLM управляют ризонингом по-разному, а у части моделей он неотключаем в принципе. Отсюда — одна ручка намерения (`reasoning: off`), отображение по провайдеру и `extra_body` как лазейка.
2. **Тройка задаётся извне, включая ключ чужой подписки.** Сегодня `api_key_env` хранит имя переменной окружения — это один ключ на весь процесс, чего достаточно оператору и не хватает пользователю, приносящему собственный ключ. Сам механизм регистрации — вне спеки по решению владельца продукта.
3. **Самохостинг убран из зоны ответственности.** Сервис моделей не запускает. LM Studio, Ollama, vLLM выставляют OpenAI-совместимый эндпоинт — сервис видит обычную тройку, и отдельного понятия «self-hosted» в нём быть не должно. Прежняя формулировка v5 («самохостед-модель как провайдер») этим отменена; уцелевшие из неё пункты — проба модели в `/healthz` и согласование возможностей — переехали в спеку как свойства любого эндпоинта.

## Принятое решение по порядку

Порядок версий — решение владельца продукта и не пересматривается. Из него следует зафиксированное окно уязвимости, которое здесь записано, чтобы о нём не забыли, а не чтобы оспорить порядок:

**с v2 подконтрольный атакующему вывод инструментов попадает в контекст, а Context Guard приезжает на v4.** Две версии этот текст живёт в промпте без семантической защиты.

Смягчение, которое не меняет порядок и обязательно к исполнению на v2 — см. `v2-dialogue-context.md`, раздел «Экранирование ≠ Context Guard»:

- **Экранирование** (v2, обязательно) защищает *целостность формата*: `tool-result` не может подделать секцию промпта переводом строки. Механика уже написана и проверена в Task 7.
- **Context Guard** (v4) защищает *семантику*: текст вида «игнорируй предыдущие инструкции» перестаёт читаться моделью как инструкция.

Это разные задачи. Экранирование на v2 — базовая гигиена формата, а не досрочный Context Guard.

## Сквозные требования ко всем версиям

Наследуются из v1 и не ослабляются:

- Fail-closed: любая ошибка, таймаут, невалидный ответ → `ask` с HTTP 200. `allow` по ошибке невозможен.
- Решение по сырой строке команды запрещено; только по `NormalizedAction`.
- Hard-deny не переопределяется ничем и не заменяется на `ask` эскалацией.
- Решение трёхзначное: `allow | deny | ask`. Hard-deny требует уверенности; когда цель действия неопределима, честный ответ — `ask`, а не отказ и не молчание.
- `deny` и `ask` не кэшируются; кэшируется только `allow`.
- Бюджет: p50 нормализации + ступени 1 ≤ 1 мс.
- Ретраев к LLM нет: один вызов, один таймаут.
